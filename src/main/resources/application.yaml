spring:
  application:
    name: order-service
#  cloud:
#    config:
#      uri:
#        - http://localhost:8084
server:
  port: 8083
eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true